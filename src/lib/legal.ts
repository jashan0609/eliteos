/**
 * Identity details used by the privacy policy and terms.
 *
 * ⚠ **These are placeholders and must be replaced before public signups open.**
 * A privacy policy naming nobody is not a privacy policy — GDPR requires an
 * identifiable data controller and a working way to contact them.
 *
 * They live in one file so replacing them is a single edit rather than a search
 * through two pages of prose.
 *
 * `LEGAL_CONTACT_EMAIL` must be an address that is actually monitored: it is
 * where erasure and access requests will arrive, and those carry deadlines.
 */

/** e.g. "Jashan Ubhi" for a sole trader, or a registered company name. */
export const LEGAL_ENTITY = "the EliteOS team";

/** Replace with a monitored address. */
export const LEGAL_CONTACT_EMAIL = "support@example.com";

/** Bump whenever the policy or terms change materially. */
export const LEGAL_UPDATED = "August 14, 2026";
